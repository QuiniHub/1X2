name: Apply Instr3 Mercado Patch

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply_instr3_patch.py'
      - '.github/workflows/apply_instr3_patch.yml'

permissions:
  contents: write

jobs:
  patch-instr3:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply instruction 3 patch
        run: python scripts/apply_instr3_patch.py
      - name: Syntax check
        run: |
          python -m py_compile actualizar_aprendizaje_ia.py generar_diario_aprendizaje.py motor_prediccion_quiniela.py
      - name: Commit final patch
        run: |
          rm -f scripts/apply_instr3_patch.py .github/workflows/apply_instr3_patch.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "feat: memoria sorpresas mercado y razonamiento especifico diario aprendizaje"
            git push
          fi
